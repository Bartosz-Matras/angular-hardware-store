import { ProductAlsoBought } from './product-also-bought';

describe('ProductAlsoBought', () => {
  it('should create an instance', () => {
    expect(new ProductAlsoBought()).toBeTruthy();
  });
});
